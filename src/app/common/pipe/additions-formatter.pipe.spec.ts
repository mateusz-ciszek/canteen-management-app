import { AdditionsFormatterPipe } from './additions-formatter.pipe';

describe('AdditionsFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdditionsFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
